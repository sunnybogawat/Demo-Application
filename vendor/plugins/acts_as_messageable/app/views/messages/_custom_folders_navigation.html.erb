<%= link_to "Create folder", new_user_custom_folder_path(current_user) %> <br/>
<% @message_box.custom_folder_names.each do |folder| %>
    <div>
        <div class="<%= custom_folder_nav_class(folder) -%>">
           > <%= link_to folder.capitalize, user_messages_path(current_user.id ,{ :folder => folder}) %>
        </div>
    </div
<% end %>
<br/><br/>